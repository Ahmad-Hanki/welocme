import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Details = () => {
    return (
        <View>
            <Text>Details but without folder</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default Details;
